# 0.3 work record

Implemented: atomic cancellable import with typed counts, source immutability, encoding selection, real/synthetic corpus tests, SFTP pause/resume/cancel/staging, native drop wiring, async file/crypto commands, Windows cold probe budgets, sync schema/preview/target binding and preservation, lazy SFTP and indexed search, four-target build definitions and offline Windows WebView2.

Evidence and limitations are tracked in VALIDATION_0.3.md and STATUS.md. macOS builds explicitly deferred by user on 21 September 2026. Direct FIDO2/Hello adapters, verified vendor encryption decoders, real hardware/cloud acceptance and timestamp log bookmarks remain incomplete; do not claim these passed.
